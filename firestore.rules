rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Production Security Rule: Authenticated Users (Google / Email & Password) can read & write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
